// src/components/presence/UserPresenceBeacon.tsx
// Presence is now handled automatically by Pusher presence channels
// in MessagingClient.tsx — no beacon needed.
export default function UserPresenceBeacon() {
  return null;
}